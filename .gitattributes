# Normalise to LF in the repository, regardless of the checkout platform.
* text=auto eol=lf

# The text-fidelity fixtures are compared byte-for-byte. They must survive a
# checkout on any platform completely untouched, so never apply EOL conversion.
packages/engine/fixtures/** -text

*.png binary
*.jpg binary
*.ico binary
*.docx binary
